version: "3.8"
services:
  app:
    build: .
    ports:
      - "8080:80" # Porta da aplicação
    depends_on:
      - db # Garante que o banco inicie antes da aplicação
    environment:
      - DB_HOST=dpg-cva3upij1k6c739eiv6g-a
      - DB_PORT=5432
      - DB_NAME=ihm_database_hj0b
      - DB_USER=ihm_database_hj0b_user
      - DB_PASSWORD=W0xFFuLHLvCAsDHTUmc1IvE5mu4syt0m
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: minha_app
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: senha123
    ports:
      - "5432:5432"
